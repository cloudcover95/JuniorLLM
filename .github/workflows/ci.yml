# Local-first. Do not email the owner on every push.
# Run on the machine: PYTHONPATH=. python tests/test_junior_aie.py
name: CI (manual only)

on:
  workflow_dispatch:

jobs:
  test:
    if: false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "disabled — use local unittest"
