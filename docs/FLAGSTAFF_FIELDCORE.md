# FieldCore → JuniorHome Flagstaff

`JuniorBitNetFieldCore` stays the default port in `ports/registry.py`.
JuniorHome `web3node/fieldcore_bridge.py` calls that name for Flagstaff field tasks.
Do not move the port registry into JuniorHome.
