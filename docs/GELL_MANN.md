# Gell-Mann + qutrit rails

λ1..λ8 generate su(3). Proofs on this box: each λ is Hermitian, Tr(λi λj)=2δij.
exp(-i θ λk / 2) is not exponentiated here (no SciPy). Use qutrit_rotate.ry01 for the real 0-1 plane (λ1-like).
QEC is 3-copy majority on {0,1,2}. Not Knill-Laflamme.
Pulses append ~/.juniorhome/lake/pulses.jsonl. awg:false fridge:false.
