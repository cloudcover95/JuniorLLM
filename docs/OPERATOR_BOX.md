# Operator box (later layer)

Collaborative AGI here = Kleene-AND across notes + optional emu.
`~/.juniorhome/operator/qutrit.ok` flips `live_hw` only. No CMOS die. No qutrit.
`emu_tick` is a cyclic |0>,|1>,|2> vector. Not SU(3).
T0 stays trit_gates. T3 stays GGUF header. This box is T2 jsonl + file gate.
