---
name: linux-os
description: Use when the slice is JuniorOS overlay, bitnetd, juniorctl, systemd, or os-release.
---
# Linux OS slice

Overlay only. No custom kernel blob. No OSWorld score claims.

Allowed: `rails/linux/*`, loopback bind, `juniorctl`, `os-release.junior`, units.
bitnetd stays `127.0.0.1:8765`.
Pi default night ticks = 32.
