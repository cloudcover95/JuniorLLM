# Asahi + MLX

Use the **stock Asahi kernel**. Install `mlx` in userspace. Point PYTHONPATH at BitNet-mlx + JuniorLLM.

Do not patch Apple AGX firmware. Overnight agents do not flash NOR.
ISO, if any: Asahi installer first, then `stage_overlay.sh` onto that root.
